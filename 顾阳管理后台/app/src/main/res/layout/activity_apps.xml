<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical" android:background="@color/background">
    <LinearLayout android:layout_width="match_parent" android:layout_height="56dp"
        android:orientation="horizontal" android:gravity="center_vertical" android:padding="8dp"
        android:background="@color/cardBg">
        <EditText android:id="@+id/etSearch" android:layout_width="0dp"
            android:layout_height="40dp" android:layout_weight="1" android:hint="搜索应用"
            android:inputType="text" android:singleLine="true" android:padding="8dp"
            android:textSize="14sp" android:background="@drawable/input_bg"/>
        <Spinner android:id="@+id/spCategory" android:layout_width="100dp"
            android:layout_height="40dp" android:layout_marginLeft="8dp"
            android:background="@drawable/input_bg"/>
    </LinearLayout>
    <ListView android:id="@+id/listApps" android:layout_width="match_parent"
        android:layout_height="0dp" android:layout_weight="1" android:divider="@color/divider"
        android:dividerHeight="1dp" android:padding="8dp"/>
    <Button android:id="@+id/btnAdd" android:layout_width="match_parent"
        android:layout_height="56dp" android:text="+ 新增应用" android:textSize="16sp"
        android:textColor="@color/onPrimary" android:background="@drawable/btn_bg"
        android:layout_margin="8dp"/>
</LinearLayout>
